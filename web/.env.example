# Copy to .env to override. adapter-node reads these at startup.
PORT=4499
HOST=0.0.0.0
# ORIGIN=https://rummy.example.com  # set in production so form actions and CSRF checks know the public URL
# CAIRN_DIR=/app/cairn/items       # where /roadmap reads items; defaults to the repo's cairn/items
